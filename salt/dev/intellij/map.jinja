{# {% import_yaml "defaults.yaml" as defaults %} #}

{% set intellij = salt['grains.filter_by']({
    'default': {
        'download_url': defaults.intellij.url,
        'destination_dir': '/opt',
        'generic_link': '/opt/default-idea',
        'owner': 'root',
        'owner_link_location': '/root/bin/idea',
        'url': "https://download.jetbrains.com/idea/ideaIC-2017.2.5.tar.gz"
    },
    'Windows': {
    },
}, merge=salt['pillar.get']('intellij')) %}
