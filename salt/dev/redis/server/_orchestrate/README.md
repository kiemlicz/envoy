Orchestrate **runner** for redis cluster.  
Redis instances are distinguished by IP:PORT pair only. This is sufficient for
all use-cases: 
 - instance per physical node or VM
 - multiple instances per physical node or VM
 -
