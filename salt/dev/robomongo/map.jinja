{% import_yaml "defaults.yaml" as defaults %}

{% set url = defaults.robomongo.url %}
{% set robomongo = salt['grains.filter_by']({
    'default': {
        'download_url': url,
        'destination_dir': '/opt',
        'generic_link': '/opt/default-robomongo',
        'owner': 'root',
        'owner_link_location': '/root/bin/robomongo',
    },
    'Windows': {
    },
}, merge=salt['pillar.get']('robomongo', default=defaults.robomongo, merge=True)) %}
