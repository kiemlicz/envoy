{% import_yaml "lxc/defaults.yaml" as defaults %}

{% set lxc = salt['grains.filter_by']({
    'default': {
        'pkgs': ["lxc", "bridge-utils", "debootstrap", "ubuntu-archive-keyring"],
    },
    'Windows': {
    }
}, merge=salt['pillar.get']('lxc', default=defaults.lxc, merge=True)) %}