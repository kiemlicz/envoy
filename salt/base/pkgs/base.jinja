{% set pkgs = salt['grains.filter_by']({
    'default': {
        'names': [
           "sudo",
           "apt-transport-https",
           "man",
           "rsync",
           "git",
           "curl",
           "ntp",
           "zsh"
        ],
    },
    'Windows': {
        'names': [
           "git"
        ],
    }
}, merge=salt['pillar.get']('base_pkgs')) %}
{% block pkgs_filter %}
{% endblock %}
